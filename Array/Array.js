let arr = [1,2,3,4,5];
console.log(arr)
arr[3] = 55
arr.push(10) // add element in array at the end of the array
arr.unshift(-1) //add element in array at the beginning of the array
console.log(arr)
arr.shift()
console.log(arr) // remove the element at 0 indexed
arr.pop() //remove the last element in the array
console.log(arr)

